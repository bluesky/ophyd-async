# show coverage in CI status, not as a comment.
comment: false
coverage:
  status:
    project:
      default:
        target: auto
        threshold: 0.05%
    patch:
      default:
        target: auto
        threshold: 0.05%
github_checks:
  annotations: false
